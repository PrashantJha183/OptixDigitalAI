[build]
  command = "npm run build"
  publish = "dist"
  environment = { NODE_ENV = "production" }

# Serve SPA routes correctly
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
